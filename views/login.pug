html
    head
        link(rel="stylesheet", href="/css/styles.css")
    main
        if error
            h1 #{error}
        else
            h1 Connectez-vous:

        form.verticalFlow(method="post")
            input(type="email", name="username", placeholder="email")
            input(type="password", name="password", placeholder="password")
            button(type="submit") Log in